apiVersion: v2
name: elearning
description: eLearning Platform — Course Service (Go) + User Service (Go) + PostgreSQL + Frontend
type: application
version: 0.3.0
appVersion: "1.0.0"
keywords:
  - elearning
  - education

dependencies:
  - name: postgresql
    version: "15.x.x"
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
