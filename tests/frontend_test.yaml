suite: frontend deployment
templates:
  - frontend/deployment.yaml
release:
  name: elearning
tests:
  - it: renders a Deployment named after the release
    asserts:
      - isKind:
          of: Deployment
      - equal:
          path: metadata.name
          value: elearning-frontend
  - it: runs the frontend container from the configured image
    asserts:
      - equal:
          path: spec.template.spec.containers[0].name
          value: frontend
      - equal:
          path: spec.template.spec.containers[0].image
          value: ghcr.io/towarmat83/elearning-frontend:latest
