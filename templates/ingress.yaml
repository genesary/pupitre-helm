{{- if .Values.ingress.enabled -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "pupitre.fullname" . }}
  labels:
    {{- include "pupitre.labels" . | nindent 4 }}
  annotations:
    {{- if eq .Values.ingress.className "nginx" }}
    nginx.ingress.kubernetes.io/use-regex: "true"
    {{- end }}
    {{- with .Values.ingress.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls }}
  tls:
    {{- toYaml .Values.ingress.tls | nindent 4 }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.host | quote }}
      http:
        paths:
          {{- if eq .Values.ingress.className "nginx" }}
          {{- include "pupitre.ingressNginxOverridePaths" . | nindent 10 }}
          {{- end }}
          - path: /api/auth
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-user-service
                port:
                  name: http
          - path: /api/settings
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-user-service
                port:
                  name: http
          - path: /api/my
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-user-service
                port:
                  name: http
          - path: /api/admin/courses
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-course-service
                port:
                  name: http
          - path: /api/admin/lab-checks
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-course-service
                port:
                  name: http
          - path: /api/admin
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-user-service
                port:
                  name: http
          - path: /api/courses
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-course-service
                port:
                  name: http
          - path: /uploads
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-course-service
                port:
                  name: http
          - path: /health
            pathType: Exact
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-course-service
                port:
                  name: http
          - path: /metrics
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-course-service
                port:
                  name: http
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "pupitre.fullname" . }}-frontend
                port:
                  name: http
{{- end }}
