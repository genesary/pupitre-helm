apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "elearning.fullname" . }}-course-service-config
  labels:
    {{- include "elearning.courseService.labels" . | nindent 4 }}
data:
  config.yaml: |
    port: {{ .Values.courseService.service.port }}
    jwtExpiryHours: {{ .Values.courseService.env.JWT_EXPIRY_HOURS }}
    corsOrigins:
      - "{{ include "elearning.publicUrl" . }}"
    uploadsDir: "/data/uploads"
    k8sNamespace: {{ .Release.Namespace | quote }}
    userServiceUrl: "http://{{ include "elearning.fullname" . }}-user-service.{{ .Release.Namespace }}.svc.{{ .Values.clusterDomain }}:{{ .Values.userService.service.port }}"
    gitCacheTtlMinutes: {{ .Values.courseService.env.GIT_CACHE_TTL }}
    gitCredentialsPath: "/etc/git-credentials/git-credentials.yaml"
    checkerServiceUrl: "http://{{ include "elearning.fullname" . }}-checker-service.{{ .Release.Namespace }}.svc.{{ .Values.clusterDomain }}:{{ .Values.checkerService.service.port }}"
