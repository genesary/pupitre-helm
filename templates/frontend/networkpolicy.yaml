{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "elearning.fullname" . }}-frontend
  labels:
    {{- include "elearning.frontend.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "elearning.frontend.selectorLabels" . | nindent 6 }}
  policyTypes: [Ingress, Egress]
  ingress:
    - ports:
        - port: {{ .Values.frontend.service.port }}
  egress:
    - to:
        - podSelector:
            matchLabels:
              {{- include "elearning.userService.selectorLabels" . | nindent 14 }}
      ports:
        - port: {{ .Values.userService.service.port }}
    - to:
        - podSelector:
            matchLabels:
              {{- include "elearning.courseService.selectorLabels" . | nindent 14 }}
      ports:
        - port: {{ .Values.courseService.service.port }}
    # DNS
    - ports:
        - port: 53
          protocol: UDP
        - port: 53
          protocol: TCP
{{- end }}
