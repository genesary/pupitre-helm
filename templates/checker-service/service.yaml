apiVersion: v1
kind: Service
metadata:
  name: {{ include "pupitre.fullname" . }}-checker-service
  labels:
    {{- include "pupitre.checkerService.labels" . | nindent 4 }}
spec:
  type: {{ .Values.checkerService.service.type }}
  ports:
    - port: {{ .Values.checkerService.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pupitre.checkerService.selectorLabels" . | nindent 4 }}
